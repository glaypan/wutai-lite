# 安卓手机服务器安装说明

> 适用：天玑6300 6GB 安卓手机（vivo 等），将手机作为便携服务器运行 wutai 舞台流程表 + vj-video 视频库，支持网页访问与远程连接。
> 作者：glaypan

## 一、准备

- 安卓手机（推荐 4GB 内存以上，6GB 最佳）
- 充电宝 / 电源（手机电池可撑 8-12 小时）
- WiFi 或手机流量

## 二、安装 Termux

1. 手机浏览器下载 Termux APK：
   `https://github.com/glaypan/wutai/releases/download/android-files-v1/termux.apk`
2. 安装 APK（设置里允许未知来源）
3. 打开 Termux

## 三、换国内镜像（加速）

在 Termux 里执行：

```
termux-change-repo
```

选择「Tsinghua TUNA 清华大学」镜像 → 回车确认。
验证：

```
pkg update
```

## 四、更新系统 + 安装 Node

```
pkg upgrade -y
pkg install -y nodejs-lts
```

验证：

```
node -v
npm -v
```

> 如果 `node -v` 报错（如 EVP_MD_CTX_get_size_ex 符号错误），先执行 `pkg upgrade -y` 修复再验证。

## 五、下载 wutai 安装包

```
curl -o ~/wutai-server.zip -L "https://github.com/glaypan/wutai/releases/download/android-files-v1/wutai-server.zip"
```

## 六、解压

```
mkdir -p ~/server
unzip -o ~/wutai-server.zip -d ~/server
```

## 七、下载改好端口的配置文件（18088-18099）

```
curl -o ~/server/wutai/config.json -L "https://raw.githubusercontent.com/glaypan/wutai/main/android-files/wutai-config.json"
```

## 八、安装依赖

```
cd ~/server/wutai
npm install
```

## 九、启动 wutai

```
cd ~/server/wutai
node server-standalone.js
```

看到以下输出即成功：

```
入口页:   http://localhost:18088/
控制端:   http://localhost:18089/?role=control (需密码)
客户端:   http://localhost:18090/
字幕屏:   http://localhost:18096/?role=screen&subtitleMode=1
提示屏:   http://localhost:18097/?role=screen
字幕控制端: http://localhost:18098/?role=control&ctrlMode=subtitle
提示屏控制端: http://localhost:18099/?role=control&ctrlMode=screen
局域网:   http://<手机IP>:18088/
```

## 十、浏览器访问

手机浏览器打开：

```
http://<手机IP>:18088/
```

（手机 IP 查看：Termux 里执行 `ifconfig` 或看 Wi-Fi 详情）

## 十一、一键启动 / 停止脚本

先创建快捷脚本目录：

```
mkdir -p ~/.shortcuts
```

下载脚本：

```
curl -o ~/.shortcuts/start-wutai.sh https://raw.githubusercontent.com/glaypan/wutai/main/android-files/start-wutai.sh
curl -o ~/.shortcuts/stop-wutai.sh https://raw.githubusercontent.com/glaypan/wutai/main/android-files/stop-wutai.sh
```

授权：

```
chmod +x ~/.shortcuts/start-wutai.sh ~/.shortcuts/stop-wutai.sh
```

用法：

```
bash ~/.shortcuts/start-wutai.sh     启动
bash ~/.shortcuts/stop-wutai.sh      停止
```

### 显示 IP 地址脚本（查看访问地址）

```
curl -o ~/.shortcuts/show-ip.sh https://raw.githubusercontent.com/glaypan/wutai/main/android-files/show-ip.sh
chmod +x ~/.shortcuts/show-ip.sh
```

用法：

```
bash ~/.shortcuts/show-ip.sh
```

会显示当前手机 IP 和所有服务访问地址。

## 十二、桌面小组件（可选）

1. 下载 termux-widget APK：`https://github.com/glaypan/wutai/releases/download/android-files-v1/termux-widget.apk`
2. 安装
3. 桌面长按 → 小组件 → Termux:Widget → 拖到桌面
4. 点击 start-wutai.sh 启动 / stop-wutai.sh 停止

## 十三、安装 vj-video 视频库（纯静态）

### 1. 下载 vj-video 源码包

```
curl -o ~/vj-video.zip https://github.com/glaypan/wutai/releases/download/android-files-v1/vj-video.zip
```

### 2. 解压到 web 目录

```
mkdir -p ~/server/web
unzip -o ~/vj-video.zip -d ~/server/web/
```

### 3. 创建静态服务器

```
mkdir -p ~/server
```

创建 `~/server/static-server.js`：

```js
const http = require('http'), fs = require('fs'), path = require('path');
const ROOT = path.join(__dirname, 'web');
const MIME = { '.html':'text/html', '.js':'text/javascript', '.css':'text/css', '.png':'image/png', '.jpg':'image/jpeg', '.mp4':'video/mp4', '.svg':'image/svg+xml' };
http.createServer((req, res) => {
  let p = req.url === '/' ? '/index.html' : req.url;
  let fp = path.join(ROOT, p);
  if (!fs.existsSync(fp)) { res.writeHead(404); res.end('404'); return; }
  res.writeHead(200, { 'Content-Type': MIME[path.extname(fp)] || 'application/octet-stream' });
  res.end(fs.readFileSync(fp));
}).listen(18092, '0.0.0.0');
console.log('vj 静态服务 http://0.0.0.0:18092');
```

### 4. 下载 vj 启动脚本

```
curl -o ~/.shortcuts/start-vj.sh https://raw.githubusercontent.com/glaypan/wutai/main/android-files/start-vj.sh
chmod +x ~/.shortcuts/start-vj.sh
```

### 5. 启动 + 访问

```
bash ~/.shortcuts/start-vj.sh
```

浏览器打开：

```
http://<手机IP>:18092/VJ-studio.html
```

## 十四、远程访问（Tailscale，手机流量/外网）

### 1. 手机 Termux 装 Tailscale

```
pkg install tailscale -y
```

### 2. 启动 + 登录

```
tailscaled &
tailscale up
```

浏览器打开提示的链接 → 手机号/QQ 登录 → 授权

### 3. 查看手机虚拟 IP

```
tailscale ip -4
```

得到类似 `100.x.x.x` 的 IP

### 4. 电脑装 Tailscale

- 下载：https://tailscale.com/download
- 登录**同一账号**

### 5. 远程访问（任何网络）

```
http://100.x.x.x:18088/       wutai 舞台流程表
http://100.x.x.x:18092/VJ-studio.html   vj 视频库
```

## 十五、端口汇总

| 端口 | 用途 |
|------|------|
| 18088 | wutai 入口页 |
| 18089 | wutai 控制端（需密码） |
| 18090 | wutai 客户端 |
| 18096 | wutai 字幕屏 |
| 18097 | wutai 提示屏 |
| 18098 | wutai 字幕控制端 |
| 18099 | wutai 提示屏控制端 |
| 18092 | vj 视频库 |

## 注意事项

- 启动前确认服务没在跑：`pkill -f server-standalone.js`
- 别重复启动（报 EADDRINUSE 说明已在运行）
- Termux 别被杀后台：设置里把 Termux 加入电池白名单
- 屏幕保持常亮（设置 → 开发者选项 → 充电时保持唤醒）

## 常见问题与解决

### 1. 命令提示 `No command found` / 命令不存在

原因：命令拼写错误（多打了斜杠、少了空格、多了逗号）。
解决：复制文档中的命令时**完整选中**，不要手打。注意：
- `bash` 不是 `/bash` 或 `bbash`
- `bash` 和路径之间**必须有空格**：`bash ~/.shortcuts/start-wutai.sh`
- `node -v` 中间有空格，不是 `node-v`

### 2. `mkdir: cannot execute binary file`

原因：之前错误的重定向操作覆盖了系统 mkdir 命令。
解决：重装 coreutils 或清除 Termux 数据重装（见问题 10）。

### 3. `E: Unable to locate package termux-widget`

原因：新版 Termux 源已下架 termux-widget。
解决：直接下载 APK 安装：
`https://github.com/glaypan/wutai/releases/download/android-files-v1/termux-widget.apk`

### 4. `E: Unable to locate package termux-tasher`

原因：拼写错误，包名是 termux-tasker（如需要）。
解决：不影响 wutai 部署，可忽略。

### 5. `listen EADDRINUSE: address already in use`

原因：服务已经在运行，又启动了一次。
解决：先停止再启动：
```
bash ~/.shortcuts/stop-wutai.sh
bash ~/.shortcuts/start-wutai.sh
```

### 6. `CANNOT LINK EXECUTABLE "node": cannot locate symbol "EVP_MD_CTX_get_size_ex"`

原因：Node 与 Termux 系统 OpenSSL 库版本不兼容。
解决：
```
pkg upgrade -y
```
升级系统库后 `node -v` 验证。

### 7. `curl: client returned ERROR on write` / 下载失败

原因：目标目录不存在（如 `~/.shortcuts` 没创建）。
解决：先创建目录：
```
mkdir -p ~/.shortcuts
```
再重新 curl 下载。

### 8. 中文文件名下载后乱码

原因：浏览器按系统默认编码打开 UTF-8 文件。
解决：用编辑器（VS Code/记事本）以 UTF-8 打开，或直接用 GitHub 网页上传。

### 9. 桌面小组件点击脚本无反应/无输出

原因：Termux:Widget 后台运行脚本，echo 输出不可见。
解决：脚本用 termux-toast 通知显示（show-ip.sh 已支持），需装：
```
pkg install termux-api -y
```

### 10. 操作混乱想重来

原因：输入太多错误命令，环境混乱。
解决：清除 Termux 数据重装：
```
设置 → 应用管理 → Termux → 存储 → 清除数据
```
然后从「二、安装 Termux」重新开始。
